[project]
name = "auto-fix-agent"
version = "0.1.0"
description = "Demo Java Web auto-fix agent (Python)"
requires-python = ">=3.8"

[project.dependencies]
pyyaml = "*"
requests = "*"
openai = "*"
gitpython = "*"
javalang = ">=0.13.0"
networkx = ">=3.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

